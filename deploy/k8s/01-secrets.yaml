# =====================================================================
# WARNING: This secret uses PLACEHOLDERS. Never commit real secrets.
# Fill values via one of:
#   1) Manual apply (preferred for first setup):
#      kubectl -n inanh24h create secret generic inanh24h-secrets \
#        --from-literal=MONGODB_URI='mongodb://...' \
#        --from-literal=ADMIN_TOKEN_SECRET='...' \
#        --from-literal=ADMIN_BOOTSTRAP_SECRET='...' \
#        --from-literal=CUSTOMER_TOKEN_SECRET='...' \
#        --from-literal=MINIO_ROOT_PASSWORD='...' \
#        --from-literal=AI_API_KEY='...' \
#        --from-literal=REVALIDATE_SECRET='...'
#   2) CI: generate this secret from GitHub Secrets in the deploy job.
# Values below are placeholders and MUST be replaced before apply.
# =====================================================================
apiVersion: v1
kind: Secret
metadata:
  name: inanh24h-secrets
  namespace: inanh24h
type: Opaque
stringData:
  MONGODB_URI: "CHANGEME"
  ADMIN_TOKEN_SECRET: "CHANGEME"
  ADMIN_BOOTSTRAP_SECRET: "CHANGEME"
  CUSTOMER_TOKEN_SECRET: "CHANGEME"
  MINIO_ROOT_USER: "admin"
  MINIO_ROOT_PASSWORD: "CHANGEME"
  AI_API_KEY: "CHANGEME"
  AI_BASE_URL: "http://192.168.53.106:20128/v1"
  REVALIDATE_SECRET: "CHANGEME"
  SMS_PROVIDER_URL: ""
  SMS_PROVIDER_TOKEN: ""
  SMS_SENDER_ID: ""
