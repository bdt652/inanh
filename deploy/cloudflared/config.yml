tunnel: YOUR_TUNNEL_ID
credentials-file: /etc/cloudflared/credentials.json

ingress:
  - hostname: inanh24h.com
    service: http://web:3000
  - hostname: api.inanh24h.com
    service: http://api:8000
  - service: http_status:404
